# 📋 Invoice Manager Package

## Опис

Повнофункціональний пакет для управління інвойсами в Laravel з сучасним фронтендом на Bootstrap 5. Пакет включає повний CRUD функціонал, генерацію PDF та email-розсилку.

## ✨ Функції

- ✅ **CRUD операції** - створення, редагування, перегляд та видалення інвойсів
- 📊 **Дашборд** - статистика та графіки
- 📄 **PDF генерація** - красиві інвойси в PDF форматі
- 📧 **Email розсилка** - автоматична відправка інвойсів клієнтам
- 🎨 **Сучасний UI** - Bootstrap 5 з адаптивним дизайном
- 📱 **Мобільна версія** - повністю адаптивний інтерфейс
- 🔍 **Пошук та фільтри** - швидкий пошук інвойсів
- 📈 **Аналітика** - статистика по статусам та сумам

## 🛠 Встановлення

### 1. Підключення пакету

Додайте до `composer.json` вашого основного проекту:

```json
{
    "autoload": {
        "psr-4": {
            "InvoiceManager\\": "packages/invoice-manager/src/"
        }
    }
}
```

Виконайте:
```bash
composer dump-autoload
```

### 2. Реєстрація Service Provider

У `config/app.php` додайте:

```php
'providers' => [
    // ... інші провайдери
    InvoiceManager\InvoiceManagerServiceProvider::class,
],
```

### 3. Встановлення залежностей

```bash
composer require barryvdh/laravel-dompdf
```

### 4. Міграції

Виконайте міграції:
```bash
php artisan migrate
```

### 5. Налаштування роутів

У `routes/web.php` додайте:
```php
require __DIR__.'/../../packages/invoice-manager/routes/web.php';
```

### 6. Конфігурація пошти

У `.env` файлі налаштуйте SMTP:
```env
MAIL_MAILER=smtp
MAIL_HOST=your-smtp-host
MAIL_PORT=587
MAIL_USERNAME=your-email
MAIL_PASSWORD=your-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=your-email
MAIL_FROM_NAME="Your Company"
```

## 📁 Структура пакету

```
packages/invoice-manager/
├── app/
│   ├── Http/Controllers/
│   │   └── InvoiceController.php
│   ├── Mail/
│   │   └── InvoiceMail.php
│   └── Models/
│       └── Invoice.php
├── database/migrations/
│   └── 2024_01_01_000000_create_invoices_table.php
├── resources/views/
│   ├── layouts/
│   │   └── app.blade.php
│   ├── invoices/
│   │   ├── index.blade.php
│   │   ├── create.blade.php
│   │   ├── edit.blade.php
│   │   ├── show.blade.php
│   │   └── pdf.blade.php
│   ├── emails/
│   │   └── invoice.blade.php
│   └── dashboard.blade.php
├── routes/
│   └── web.php
├── src/
│   └── InvoiceManagerServiceProvider.php
├── composer.json
└── README.md
```

## 🚀 Використання

### Основні роути:

- `GET /dashboard` - Дашборд
- `GET /invoices` - Список інвойсів
- `GET /invoices/create` - Створення інвойсу
- `GET /invoices/{id}` - Перегляд інвойсу
- `GET /invoices/{id}/edit` - Редагування інвойсу
- `GET /invoices/{id}/pdf` - Завантаження PDF
- `POST /invoices/{id}/send` - Відправка email

### Приклад використання в контролері:

```php
use App\Models\Invoice;

// Створення інвойсу
$invoice = Invoice::create([
    'number' => 'INV-001',
    'client' => 'Іван Петренко',
    'client_email' => 'ivan@example.com',
    'amount' => 1500.00,
    'due_date' => '2024-02-01',
    'status' => 'draft'
]);

// Генерація PDF
$pdf = Pdf::loadView('invoices.pdf', compact('invoice'));
return $pdf->download('invoice.pdf');

// Відправка email
Mail::to($invoice->client_email)->send(new InvoiceMail($invoice));
```

## 🎨 Особливості UI

### Bootstrap 5 Компоненти:
- Адаптивні таблиці
- Модальні вікна
- Dropdown меню
- Алерти та повідомлення
- Прогрес-бари
- Картки (Cards)

### Кольорова схема:
- **Основний**: #3498db (синій)
- **Вторинний**: #2c3e50 (темно-сірий)
- **Успіх**: #27ae60 (зелений)
- **Попередження**: #f39c12 (помаранчевий)
- **Небезпека**: #e74c3c (червоний)

### Font Awesome іконки:
Використовуються іконки FA 6.0 для покращення UX

## 📊 Статуси інвойсів

- **draft** (Чернетка) - інвойс створено, але не відправлено
- **sent** (Відправлено) - інвойс відправлено клієнту
- **paid** (Оплачено) - інвойс оплачено

## 🔧 Налаштування

### Зміна логотипу компанії:
У файлі `pdf.blade.php` змініть інформацію про компанію.

### Налаштування email шаблону:
Відредагуйте `emails/invoice.blade.php` для кастомізації листа.

### Додаткові поля:
Додайте поля до міграції та моделі за потреби.

## 🐛 Відладка

Якщо виникають проблеми:

1. Перевірте, чи виконані міграції
2. Перевірте автозавантаження класів
3. Переконайтесь у правильності налаштувань пошти
4. Перевірте права доступу до файлів

## 📝 Логи

Логи email відправки зберігаються в `storage/logs/laravel.log`

## 🤝 Підтримка

Для питань та пропозицій створюйте Issues або Pull Requests.

## 📄 Ліцензія

MIT License

---

**Розроблено з ❤️ для Laravel спільноти**